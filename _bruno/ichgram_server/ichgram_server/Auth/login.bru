meta {
  name: login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username_email": "tester",
    "password": "123456"
  }
}

script:post-response {
  if (res.body.token) {
    bru.setVar('token', res.body.token);
  }
}
