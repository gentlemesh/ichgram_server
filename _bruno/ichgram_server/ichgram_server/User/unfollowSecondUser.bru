meta {
  name: unfollowSecondUser
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/user/unfollow/{{secondUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
