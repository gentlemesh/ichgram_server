meta {
  name: followSecondUser
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/user/follow/{{secondUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
