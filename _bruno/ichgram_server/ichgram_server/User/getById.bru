meta {
  name: getById
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/user/{{firstUserId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
