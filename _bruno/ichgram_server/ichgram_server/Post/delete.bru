meta {
  name: delete
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/post/{{firstPostId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
