meta {
  name: Like
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/post/{{firstPostId}}/like
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
